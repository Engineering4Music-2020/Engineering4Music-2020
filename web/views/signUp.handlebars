<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Engineering4Music: Sign Up</title>
    <link href="css/main.css" rel="stylesheet" title="Default Style" />
    <script src="../dist/web/src/controllers/register.js"></script>
</head>

<body>
    <div class="login-form">
        <h1>{{title}}</h1>
        <form action="auth" method="post">
            <input type="text" name="raspiId" placeholder="Type in the number written on your RaspberryPi" required>
            <input type="text" name="name" placeholder="Name" required>
            <input type="text" name="email" placeholder="E-Mail" required>
            <input type="password" name="password" placeholder="Password" required>
            <input type="submit" value="Register">
        </form>
        {{#if sessionFlash.message}}
        <div id="flash-messages" class="container">
            {{#contains sessionFlash.type "warning"}}
            <div class="message">{{{sessionFlash.message}}}</div>
            {{/contains}}
            {{#contains sessionFlash.type "danger"}}
            <div class="message">{{{sessionFlash.message}}}</div>
            {{/contains}}
            {{#contains sessionFlash.type "success"}}
            <div class="message">{{{sessionFlash.message}}}</div>
            {{/contains}}
        </div>
        {{/if}}
    </div>
</body>

</html>